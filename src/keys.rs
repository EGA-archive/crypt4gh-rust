use rpassword::read_password;
use std::path::Path;

pub fn get_private_key(key_path: &Path, passphrase: String) -> Vec<u8> {
	unimplemented!()
}

pub fn get_public_key(key_path: &Path) -> Vec<u8> {
	unimplemented!()
}
